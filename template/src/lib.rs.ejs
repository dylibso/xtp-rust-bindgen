mod pdk;
use pdk::*;

use extism_pdk::*;

<% schema.exports.forEach(ex => { -%>

pub(crate) fn <%= camelToSnakeCase(ex.name) %>(<%if (ex.input) { %>_input: <%- toRustType(ex.input) %> <% } %>) -> Result<<%if (ex.output) { %> <%- toRustType(ex.output) %> <% } else { %> () <% } %>, Error> {
    todo!("<%= camelToSnakeCase(ex.name) %>")
}

<% }) %>
